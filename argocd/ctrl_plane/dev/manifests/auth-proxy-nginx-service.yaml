apiVersion: v1
kind: Service
metadata:
  name: auth-proxy-nginx
  namespace: customer-observability
spec:
  selector:
    app.kubernetes.io/name: nginx
  ports:
    - port: 80
      targetPort: 3000
  type: LoadBalancer
