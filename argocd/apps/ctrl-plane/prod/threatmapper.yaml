# ThreatMapper Management Console — production.
# See dev/threatmapper.yaml for detailed comments.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: threatmapper
  namespace: argocd
spec:
  project: default
  source:
    chart: deepfence-console
    repoURL: https://deepfence-helm-charts.s3.amazonaws.com/threatmapper
    targetRevision: 2.4.0
    helm:
      releaseName: threatmapper
      valuesObject:
        global:
          storageClass: "standard-rwo"

        console:
          replicas: 2
          resources:
            requests:
              cpu: "2"
              memory: "4Gi"
            limits:
              cpu: "4"
              memory: "8Gi"
          nodeSelector:
            node_pool: security
          service:
            type: ClusterIP

        router:
          replicas: 2
          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"
            limits:
              cpu: "1"
              memory: "1Gi"
          nodeSelector:
            node_pool: security

        ingress:
          enabled: true
          annotations:
            cert-manager.io/cluster-issuer: "letsencrypt-prod"
            kubernetes.io/ingress.class: nginx
          hosts:
            - host: threatmapper.security.internal.falkordb.cloud
              paths:
                - path: /
                  pathType: Prefix
          tls:
            - secretName: threatmapper-tls
              hosts:
                - threatmapper.security.internal.falkordb.cloud

        postgres:
          resources:
            requests:
              cpu: "1"
              memory: "2Gi"
            limits:
              cpu: "2"
              memory: "4Gi"
          nodeSelector:
            node_pool: security
          storage:
            size: 50Gi

        redis:
          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"
            limits:
              cpu: "1"
              memory: "1Gi"
          nodeSelector:
            node_pool: security

        neo4j:
          resources:
            requests:
              cpu: "1"
              memory: "2Gi"
            limits:
              cpu: "2"
              memory: "4Gi"
          nodeSelector:
            node_pool: security
          storage:
            size: 50Gi

        kafka:
          resources:
            requests:
              cpu: "1"
              memory: "2Gi"
            limits:
              cpu: "2"
              memory: "4Gi"
          nodeSelector:
            node_pool: security
          storage:
            size: 100Gi

  destination:
    server: https://kubernetes.default.svc
    namespace: security
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
    automated:
      prune: true
      selfHeal: true
  ignoreDifferences:
    - group: ""
      kind: Secret
      jsonPointers:
        - /data
