# Wazuh Manager — central SIEM/XDR hub (production).
# See dev/wazuh.yaml for detailed comments.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: wazuh
  namespace: argocd
spec:
  project: default
  source:
    chart: wazuh
    repoURL: https://packages.wazuh.com/4.x/helm/
    targetRevision: 4.9.2
    helm:
      releaseName: wazuh
      valuesObject:
        wazuh:
          manager:
            replicas: 3
            resources:
              requests:
                cpu: "2"
                memory: "4Gi"
              limits:
                cpu: "4"
                memory: "8Gi"
            nodeSelector:
              node_pool: security
            config:
              auth:
                use_source_ip: "no"
                force_insert: "yes"
                force_time: 0
                purge: "yes"
                use_password: "yes"
                ssl_agent_ca: /var/ossec/etc/sslmanager.ca
                ssl_verify_host: \"yes\"
                ssl_auto_negotiate: \"yes\"
              vulnerability-detector:
                enabled: "yes"
                interval: 5m
                run_on_start: "yes"
                providers:
                  - name: canonical
                    enabled: "yes"
                  - name: debian
                    enabled: "yes"
                  - name: redhat
                    enabled: "yes"
                  - name: nvd
                    enabled: "yes"
              syscheck:
                frequency: 43200
                scan_on_start: "yes"
                directories:
                  - path: /etc
                    check_all: "yes"
                    realtime: "yes"
                  - path: /usr/bin
                    check_all: "yes"
                  - path: /usr/sbin
                    check_all: "yes"
                  - path: /bin
                    check_all: "yes"
              # Google Chat integration — webhook URL from sealed secret
              integration:
                - name: custom-google-chat
                  hook_url: "FROM_SECRET"
                  level: 10
                  alert_format: "json"
            # Mount custom SOC 2 rules and integrations from ConfigMaps
            extraEnvFrom:
              - secretRef:
                  name: wazuh-google-chat-webhook
            extraVolumes:
              - name: custom-rules
                configMap:
                  name: wazuh-custom-rules
              - name: google-chat-integration
                configMap:
                  name: wazuh-google-chat-integration
                  defaultMode: 0755
            extraVolumeMounts:
              - name: custom-rules
                mountPath: /var/ossec/etc/rules/local_rules.xml
                subPath: local_rules.xml
                readOnly: true
              - name: google-chat-integration
                mountPath: /var/ossec/integrations/custom-google-chat
                subPath: custom-google-chat
                readOnly: true
              - name: google-chat-integration
                mountPath: /var/ossec/integrations/custom-google-chat.py
                subPath: custom-google-chat.py
                readOnly: true
            persistence:
              enabled: true
              storageClass: "standard-rwo"
              size: 100Gi
            service:
              type: LoadBalancer
              loadBalancerIP: "${WAZUH_STATIC_IP}"
              annotations:
                cloud.google.com/load-balancer-type: "External"
              ports:
                - name: agents-events
                  port: 1514
                  protocol: TCP
                  targetPort: 1514
                - name: agent-enrollment
                  port: 1515
                  protocol: TCP
                  targetPort: 1515
                - name: syslog-tcp
                  port: 514
                  protocol: TCP
                  targetPort: 514
                - name: api
                  port: 55000
                  protocol: TCP
                  targetPort: 55000

          indexer:
            replicas: 3
            resources:
              requests:
                cpu: "2"
                memory: "4Gi"
              limits:
                cpu: "4"
                memory: "8Gi"
            nodeSelector:
              node_pool: security
            persistence:
              enabled: true
              storageClass: "standard-rwo"
              size: 200Gi

          dashboard:
            replicas: 2
            resources:
              requests:
                cpu: "500m"
                memory: "1Gi"
              limits:
                cpu: "1"
                memory: "2Gi"
            nodeSelector:
              node_pool: security
            extraVolumes:
              - name: soc2-dashboard
                configMap:
                  name: wazuh-dashboard-soc2
            extraVolumeMounts:
              - name: soc2-dashboard
                mountPath: /usr/share/wazuh-dashboard/config/soc2-dashboard.ndjson
                subPath: soc2-dashboard.ndjson
                readOnly: true
            lifecycleHooks:
              postStart:
                exec:
                  command:
                    - /bin/sh
                    - -c
                    - |
                      sleep 30 && curl -sS -k -XPOST \
                        "https://localhost:5601/api/saved_objects/_import?overwrite=true" \
                        -H "osd-xsrf: true" \
                        --form file=@/usr/share/wazuh-dashboard/config/soc2-dashboard.ndjson \
                        || true
            ingress:
              enabled: true
              annotations:
                cert-manager.io/cluster-issuer: "letsencrypt-prod"
                kubernetes.io/ingress.class: nginx
                # IAP — oauth2-proxy enforces Google OIDC + group membership
                nginx.ingress.kubernetes.io/auth-url: "https://auth.security.internal.falkordb.cloud/oauth2/auth"
                nginx.ingress.kubernetes.io/auth-signin: "https://auth.security.internal.falkordb.cloud/oauth2/start?rd=$scheme://$host$escaped_request_uri"
                nginx.ingress.kubernetes.io/auth-response-headers: "X-Auth-Request-User,X-Auth-Request-Email"
              hosts:
                - host: wazuh.security.internal.falkordb.cloud
                  paths:
                    - path: /
                      pathType: Prefix
              tls:
                - secretName: wazuh-dashboard-tls
                  hosts:
                    - wazuh.security.internal.falkordb.cloud

  destination:
    server: https://kubernetes.default.svc
    namespace: security
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
      - RespectIgnoreDifferences=true
    automated:
      prune: true
      selfHeal: true
  ignoreDifferences:
    - group: ""
      kind: Secret
      jsonPointers:
        - /data
