apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: security
resources:
  - ../../base
configMapGenerator:
  - name: falco-env
    behavior: create
    literals:
      - cluster-name=app-plane-dev
      - plane=app-plane
      - env=dev
generatorOptions:
  disableNameSuffixHash: true
