apiVersion: v1
kind: Service
metadata:
  name: auth-proxy
  namespace: customer-observability
spec:
  selector:
    app: auth-proxy
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
