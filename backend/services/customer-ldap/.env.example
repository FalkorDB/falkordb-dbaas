# Environment
NODE_ENV=development
PORT=3013

# Omnistrate Configuration
OMNISTRATE_EMAIL=your-omnistrate-email@example.com
OMNISTRATE_PASSWORD=your-omnistrate-password
OMNISTRATE_SERVICE_ID=your-service-id
OMNISTRATE_ENVIRONMENT_ID=your-environment-id

# JWT Secret for Session Cookies
JWT_SECRET=your-secure-random-secret-key

# GCP Configuration (for GCP clusters)
APPLICATION_PLANE_GOOGLE_CLOUD_PROJECT=your-gcp-project-id

# AWS Configuration (for AWS clusters)
AWS_TARGET_AUDIENCE=your-aws-target-audience
AWS_ROLE_ARN=arn:aws:iam::account-id:role/role-name

# Service Name
SERVICE_NAME=customer-ldap

# CORS Configuration
# Use '*' to allow all origins, or comma-separated list of specific origins
# Example: https://app.example.com,https://admin.example.com
CORS_ORIGINS=*

# Timeout Configuration (in milliseconds)
REQUEST_TIMEOUT_MS=30000
LDAP_CONNECTION_TIMEOUT_MS=10000
K8S_PORT_FORWARD_TIMEOUT_MS=15000
