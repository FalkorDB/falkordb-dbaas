apiVersion: operator.victoriametrics.com/v1beta1
kind: VMRule
metadata:
  namespace: observability
  name: fluent-bit.rules
  labels:
    is_logs: "false"
spec:
  groups:
  - name: fluent-bit
    rules:
    - alert: FluentBitError
      expr: rate(fluentbit_output_errors_total[5m]) > 0
      for: 5m
      labels:
        severity: critical
        cluster: '{{ $labels.cluster }}'
      annotations:
        summary: "Fluent Bit errors detected"
        description: "[cluster={{ $labels.cluster }}] Fluent Bit is experiencing errors"  
        runbook_url: "https://runbooks.falkordb.cloud/alerts/fluentbiterrorrunbook"
    - alert: NoOutputBytesProcessed
      expr: rate(fluentbit_output_proc_bytes_total[5m]) == 0
      annotations:
        message: |
          [cluster={{ $labels.cluster }}]  Fluent Bit instance output plugin has not processed any
          bytes for at least 15 minutes.
        summary: No Output Bytes Processed
        runbook_url: "https://runbooks.falkordb.cloud/alerts/nooutputbytesprocessedrunbook"
      for: 15m
      labels:
        severity: warning
        cluster: '{{ $labels.cluster }}'
